#!/bin/bash
# manage isolated MarkovSound sessions
set -eu
here="$(cd "$(dirname "$0")" && pwd)"
cd "$here"
exec env PYTHONPATH="$here/src${PYTHONPATH:+:$PYTHONPATH}" python3 -m markovsound.cli_session "$@"
